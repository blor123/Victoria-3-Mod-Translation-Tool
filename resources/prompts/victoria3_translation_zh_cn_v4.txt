此 ZIP 包含 Victoria 3 模组的本地化 YML 文件。

规则：
1. 不要更改本地化键、文件名或文件夹结构。
2. 保留 YAML 结构以及首行语言声明。
3. 仅将引号内玩家可见的自然语言翻译为目标语言。
4. 必须原样保留 $VARIABLE$、[Concept(...)]、#tooltips!、换行代码及所有游戏语法。
5. 处理全部 YML 文件，并尽可能保留编码和行结构。
6. 完成后以相同文件夹结构打包为 ZIP 返回。
